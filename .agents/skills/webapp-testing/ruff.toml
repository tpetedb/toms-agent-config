# The scripts, the examples and the Python in SKILL.md are vendored verbatim
# from anthropics/skills (THIRD_PARTY.md), so this repository's lint and format
# rules skip them.
extend = "../../../pyproject.toml"
extend-exclude = ["scripts", "examples", "SKILL.md"]
