`.agents/config.toml` is the one knob file, with a comment on every key, and the tables it points at ship complete under `.agents/config/`: three profiles (native delegation off in enterprise, guarded in standard and yolo), models, six role charters, teams with models, skills and a cost cap, the standards registry, a gate pack per project kind, hooks, policy, MCP, network, runtime and GitHub. `tac config show` prints every effective value with its file and line, `tac explain <key>` adds when it applies and its comment, and `tac config check --base <ref>` refuses a floor in `.agents/standards.floor.toml` that loosens any key or a waiver that has expired or lacks the owner's approval.
