`dev/ledger`, the small real tool acceptance 8 is proved against: `ledger ingest <csv> --db <file>` validates a CSV of expenses against `dev/ledger/schema.json`, refuses drift with exit 3 naming the columns, and stores the rows in DuckDB once per file; `ledger report --month YYYY-MM` prints the monthly totals. It has its own uv project at 0.1.0, lock, tests, justfile gates, Keep a Changelog file, ADR and diagram. `dev/out/` is ignored.
