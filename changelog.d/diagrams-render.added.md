`docs/diagrams/` now holds the twelve diagrams of the inventory, one per lifecycle and one per enabled pipeline, in the ISO 5807 house subset its README documents with the 42010 review checklist. `just diagrams-render` renders every diagram and every mermaid block in `docs/` with the mermaid-cli version `mise.toml` pins and refreshes `docs/diagrams/render.lock`; `just diagrams-check` and `tac check` hold the sources to that lock and the inventory to `github.toml` without node, and CI renders on every pull request. No SVG is committed.
