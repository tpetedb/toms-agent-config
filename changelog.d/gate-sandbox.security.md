The runner now starts every gate, and its own `git status` in the candidate tree, under a macOS Seatbelt profile that denies the controller store, the signing key, the runner socket and renaming any folder above the store, with a fixed environment that carries no token. Code a builder wrote (a recipe body, an order's criterion, the tests `just verify` starts) can no longer read the key. A gate that changes the checkout gets no receipt, and where no sandbox exists the runner refuses to gate rather than run unsandboxed.
