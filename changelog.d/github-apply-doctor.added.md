`tac github apply` checks the machine account (write, never admin), then creates or updates the default-branch ruleset with bypass off for everyone, code-owner review, the CI jobs as required checks from GitHub Actions only, linear history and squash-only merges, and reads it back by id; it refuses to run inside an agent session and has a `--dry-run`. `tac doctor` now fetches each active branch ruleset by id and fails on a missing or non-empty `bypass_actors`, a tag-only ruleset, or a ruleset without code-owner review or required checks, reading anonymously inside an agent session so the owner's token never enters one, and reports each client found (Claude Code, Codex), its version from `--version` alone, and whether this checkout is trusted.
