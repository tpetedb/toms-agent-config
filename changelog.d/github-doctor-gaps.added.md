The ruleset `tac github apply` sends, and every recorded GitHub reply the tests serve, are now checked against GitHub's published OpenAPI description, and the ruleset requires the last push to be approved by someone other than its pusher. `tac doctor` fails when gh inside an agent session is logged in as an admin identity, reports Codex hook trust apart from project trust, and `tac github apply --bot` defaults to `[governance] agent_identity` in `.agents/config.toml`; the runner signs a `trust` probe of the client's own config.
