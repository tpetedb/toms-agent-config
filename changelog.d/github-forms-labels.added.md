Issue forms for an order request, a bug, a feature, a decision record and a question for the owner, with blank issues off; the labels declared once in `.github/labels.yml`, planned with `tac github labels --dry-run` and applied only by the owner with `--apply`; `tac github lint`; a pull request template carrying the `order: <id>` tag line, the acceptance table and the one merge command; the base revision's checker judging the floor and the generated files in CI; and a macOS job that runs the Seatbelt tests without being a required check.
