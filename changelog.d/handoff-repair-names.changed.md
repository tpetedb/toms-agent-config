The contract repair template is now `templates/handoffs/contract-repair.md.j2`, so it no longer shares a name with the work-order repair handoff that `tac work repair` renders for a failed check. The design now lists the board-question and release-plan contracts, the envelope's `envelope_version`, `template`, `role` and `validation.repair_input_sha256` fields, the `dispatched` status and how a repair's outcome maps to degraded or escalated, and acceptance 2 names `tests/test_contracts.py`, which holds the strict-subset lint.
