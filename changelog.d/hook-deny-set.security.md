The hook guard now refuses the same paths the rendered Claude rules deny: its secret-read check covers the credential folders, the folder of the secrets env file and the controller store (`~/.local/state/tac`), not only `policy.paths.deny_read`, and its generated-paths check refuses an edit in the controller store. One pair of functions in `src/tac/adapters.py` builds the lists for both, and a test holds the guard to the rendered `.claude/settings.json`.
