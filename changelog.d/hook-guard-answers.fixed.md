The hook guard now puts every reminder where the model reads it: before a tool call and after a Codex tool call a reminder is `additionalContext` at exit 0, since both clients send stderr at exit 0 only to a debug log, and only a refusal replaces a Codex tool result. When the guard or its checker fails at a stop, it still lets the stop through and now shows why as a `systemMessage` on both clients, as Codex takes only JSON on a stop's stdout.
