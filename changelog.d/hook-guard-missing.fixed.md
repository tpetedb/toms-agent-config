A rendered Claude or Codex hook that cannot find the stamped guard, such as a Codex session outside a git work tree, now refuses a tool call with "run tac init" and lets every other event pass, instead of sending a stop back on every turn. The slowest tests (the git hooks fixture, the CI receipt and work scripts, the sandboxed gate matrices) carry the `slow` marker, so the pre-push fast set runs in about 75 seconds instead of two minutes.
