The hook guard, `hooks/run.py`, stamped to `.agents/hooks/run.py`: a stdlib guard that runs on Python 3.9, refuses to run unless started isolated with `python -I` from a plain interpreter, checks that nothing it imported came from the checkout and that every file beside it matches the new `[hooks]` section of `generated.lock`, and then asks `tac hook run` from the non-editable `.agents/.venv` for a verdict, with a fixed environment and a refusal of any `tac` imported from elsewhere. It answers Claude's PreToolUse, PostToolUse, Stop, SubagentStop and SessionStart and Codex's PreToolUse, PostToolUse, Stop and SessionStart as each documents, and before a tool call it refuses whenever it cannot vouch for itself or its checker. `tac doctor` names a guard that was changed but never stamped.
