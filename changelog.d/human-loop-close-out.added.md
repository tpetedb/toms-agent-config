`tac human ask --from-handoff` queues the escalation `tac handoff validate` writes as a question in its own section of `TODO.HUMAN.md`, with only its fixed text on the page; `tac recap` is the top-level name of `tac human recap`; `tac human verify` reads every page of a pull request's reviews; and a pre-commit hook refuses a staged `TODO.HUMAN.md` that is not the render of `.human/`.
