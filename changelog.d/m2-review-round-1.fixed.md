The M2 acceptance commands now name test files that exist: the guard's isolation proof lives in `tests/test_hook_isolation.py`, which also reaches the sys.path and imported-module refusals behind `-I`, and Acceptance 4 names `test_approvals.py` and `test_github_forms.py`. Approval checks gain tests for a bot review under the owner's login and a repository the agent identity owns, the Python 3.9 guard test no longer installs an interpreter during the run (CI installs 3.9 first), and DESIGN section 7 says single use of an approval is enforced by the runner's store in M3.
