The commit-msg hook runs the private scan on the message with a new `--message FILE` mode, which reads git's message file as the hook gets it, comment lines included, up to the scissors line; the prek fixture copies the scan's script and term list with the wrapper and proves a term in a staged file and in a message are both refused. Q21 and Q22 move into `.human/` under a new `history` section, which the owner-question form offers too, and Q21 names the merge that brings the older list into this branch.
