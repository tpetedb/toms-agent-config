CI now holds each committed receipt to `.agents/config/receipts.toml` as committed at the base: the stage must be one the acceptance gate knows, the run must be the order, and the receipt must show that stage's command and passing result. A receipt counts only while the head is its revision plus receipts, so a code commit after the gate makes it stale, and every order a change touches must carry the stages the base lists as required.
