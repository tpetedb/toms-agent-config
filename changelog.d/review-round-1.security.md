The strict-subset lint for handoff contracts now walks every place a subschema can sit (oneOf, allOf, tuple and single items, schema-valued additionalProperties, patternProperties and the conditional keywords) and refuses every keyword outside OpenAI's supported list, so a loose object hidden under oneOf fails `tac check` instead of Codex `--output-schema`. `tac check` treats all of `.codex/` as generated and names a hand edit whose lock line was rewritten to match, the Claude sandbox denies writes to `.codex/`, and the scout's Claude seat is pinned to `claude-haiku-4-5-20251001` with no effort, since Haiku 4.5 takes none.
