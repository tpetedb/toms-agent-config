CI judges the standards floor with the base revision's own checker (`scripts/ci_config_from_base.sh` in the required `work` job), so a pull request that loosens the floor and rewrites its checker is refused; Codex native subagents render off in every profile until M2 renders and proves the PreToolUse handoff guard; and the strict-subset lint enforces the documented Structured Outputs size limits (5000 properties, 120,000 characters of names and enum and const values, 1000 enum values, 15,000 characters in one string enum over 250 values).
