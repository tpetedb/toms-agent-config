The runner serves only from its own venv in the controller store (`tac runner install`, `just runner-install`, and a bootstrap step outside agent sessions) and refuses to start with an agent client among its parent processes. CI now judges committed receipts with the base revision's own verifier as well as its `runner.pub` (`scripts/ci_verify_receipts.sh`), `tac doctor` reports whether `runner.pub` holds a key, and `contracts/receipt.schema.json` is generated from the receipt models by `tac receipt schema`.
