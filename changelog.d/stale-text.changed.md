Text that still called decided questions open now states the decisions of ADR 0002: the `convention` and `data` comments in `config/standards.toml` (Q4, Q13), the design's `agents.env` location (Q17), the commitizen, towncrier and Soda notes in its toolchain (Q4, Q13) and the ponytail and private platform lines of its reuse list (Q1, Q19). The CI comment on gate scripts names the bootstrap pull request #6, and `pyproject.toml` declares `license = "MIT"`.
