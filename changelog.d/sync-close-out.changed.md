`tac work` now lets every order touch `.agents/generated.lock` and each output it lists, filtered through the sync allowlist so a lock edited to name another file widens nothing, instead of `teams.toml` naming generated paths by hand. Profiles and the host policy accept only the Codex `approval_policy` values the config reference still lists, `on-request` and `never`: `untrusted` is unsupported there and `on-failure` deprecated. Sync idempotence has its own test file, `tests/test_sync_idempotent.py`, as the M1 acceptance names it.
