`.agents/config/teams.toml` is read through pydantic models that refuse a value of the wrong type as well as an unknown key; `tac doctor` fails when the stamped `.agents/lib/tac` differs from `src/tac`; the work-order decision record, the `work-order` skill and `THIRD_PARTY.md` carry the port's provenance. An order marked `reference = true`, like `work/orders/example`, is read and validated but never active and never judged by CI.
