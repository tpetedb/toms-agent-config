`tac work`, ported from vibe-map: orders with owned files and command criteria, validate, check, packet, review (the review gate: current, by someone else, from the other provider), accept, repair (the handoff for the failing criteria, then the check again; the bounded builder turn waits for `tac launch`), plan, board, ci, sweep, the issue commands and the hooks, with the teams and knobs in `.agents/config/teams.toml`, the `just work-*` recipes, templates and a `work` job in CI.
