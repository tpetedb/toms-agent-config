The handoff guard now covers Claude Code's Workflow tool, which ultracode uses to start agents: a charter says `delegates = true | false`, the builder, reviewer, manager and scout agent files disallow Agent, Task and Workflow and the guard refuses them, a Workflow call runs only a script a stage of the caller's role registers under `workflows` with its sha256 in `generated.lock` and is recorded in the worker store first, `native_delegation = "off"` denies Workflow too, and `tac check` refuses `off` while a role launches with ultracode.
