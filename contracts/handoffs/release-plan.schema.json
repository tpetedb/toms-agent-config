{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "release-plan",
  "description": "What a release will be: the version, the bump and the notes, checked against changelog.d before any tag.",
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?$",
      "description": "The next version, semver 2.0.0."
    },
    "bump": {
      "type": "string",
      "enum": [
        "major",
        "minor",
        "patch"
      ],
      "description": "Which part changes, from the fragment types."
    },
    "fragments": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The changelog.d fragment files the release assembles."
    },
    "notes": {
      "type": "string",
      "description": "The release notes summary, in plain words."
    }
  },
  "required": [
    "version",
    "bump",
    "fragments",
    "notes"
  ],
  "additionalProperties": false
}
