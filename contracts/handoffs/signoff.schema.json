{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "signoff",
  "description": "A team manager's decision on a change that touches the team's paths.",
  "type": "object",
  "properties": {
    "order_id": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]{2,48}$",
      "description": "The order the sign-off is for."
    },
    "team": {
      "type": "string",
      "description": "The team signing."
    },
    "decision": {
      "type": "string",
      "enum": [
        "accept",
        "reject"
      ],
      "description": "Whether the team accepts the change to its files."
    },
    "reason": {
      "type": "string",
      "description": "Why, in one or two sentences."
    }
  },
  "required": [
    "order_id",
    "team",
    "decision",
    "reason"
  ],
  "additionalProperties": false
}
